import UserLogout from "@reusable/UserLogout";

const RecruiterLogout = () => {
  // Reusable Component that handles user logout based on user type
  return <UserLogout userType="recruiter" />;
};

export default RecruiterLogout;
